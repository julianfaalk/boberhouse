# Copy this file to docker/app.env and fill in the real values before starting the container.

# Randomly generated token shared with the iOS client.
SERVER_API_TOKEN=replace-with-server-token

# Absolute path inside the container to the APNS .p8 key file mounted read-only.
APNS_AUTH_KEY_PATH=/secrets/AuthKey_3K6UAYN79K.p8

# Apple Push Notification credentials.
APNS_KEY_ID=3K6UAYN79K
APNS_TEAM_ID=U236T5A5U4
APNS_BUNDLE_ID=com.julianfalk.BoberHouse
APNS_ENV=production

# Optional: tweak Vapor logging
# LOG_LEVEL=info
